import { ActionButton } from '.';

export default {
  title: 'Action Button',
  component: ActionButton,
};

export const Post = () => <ActionButton>Post</ActionButton>;

export const Chat = () => <ActionButton>Chat</ActionButton>;
